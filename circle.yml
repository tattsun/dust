machine:
  services:
    - docker

dependencies:
  override:
    - docker pull tattsun/dustdevenv

test:
  override:
    - >
      docker run -v $(pwd):/app tattsun/dustdevenv bash -c
      "
      cd /app;
      mkdir build;
      cd build;
      cmake .. && make && make test
      "